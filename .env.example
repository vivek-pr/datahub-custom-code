# Sample environment overrides for the UI ingestion runner
# Use http://datahub-gms:8080 when running inside Docker Compose. Switch to
# http://localhost:8080 when running the runner directly on the host.
DATAHUB_GMS_URI=http://datahub-gms:8080
# Optional: personal access token used for GraphQL calls to GMS
DATAHUB_TOKEN=
# Actor urn attached to GraphQL requests and ingestion run events
DATAHUB_ACTOR=urn:li:corpuser:ui_ingestion_runner
HEALTH_CHECK_PATHS=/admin,/api/graphiql,/api/graphql,/actuator/health,/health
